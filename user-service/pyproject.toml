[project]
name = "user-service"
version = "0.1.0"
description = "User microservice"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "sqlalchemy>=2.0.52",
    "uvicorn",
]
